HIRO's トレンドインサイト（AI連携版）

起動手順:
1) Node.js 18+ をインストール
2) このフォルダで:
   npm install
3) .env.example を .env にコピーして OPENAI_API_KEY を設定
4) 起動:
   npm run dev
5) ブラウザ:
   http://localhost:3000

注意:
- サーバー側で OpenAI Responses API を使用します。
- web_search ツールを使って公開情報を参照し、JSONでレポートを返します。
